---
title: 'REST API'
description: 'HTTP endpoints for Vero OMS integrations'
mode: 'wide'
---

<div style={{ width: '100%', height: 240, marginBottom: 32, borderRadius: 16, overflow: 'hidden', position: 'relative' }}>
  <video style={{ width: '100%', height: '100%', objectFit: 'cover' }} autoPlay loop muted playsInline>
    <source src="/images/bg_video.mp4" type="video/mp4" />
  </video>
</div>

Vero OMS REST API exposes authentication, account, order, market data, notification, referral, diagnostic and static asset endpoints.

<CardGroup cols={3}>
  <Card title="REST endpoints" icon="route">
    Use the REST reference for request parameters, response schemas and examples.
  </Card>
  <Card title="Base URL" icon="globe">
    `https://api-gw.verolabs.co/api/v1`
  </Card>
  <Card title="Coverage" icon="route">
    44 HTTP operations are available in the REST API reference.
  </Card>
</CardGroup>

## Authentication

Most trading and account endpoints use `Authorization: Bearer <jwt>`. Ory setup endpoints use native login, registration, recovery and settings flows; selected Ory endpoints use `X-Session-Token`.

## Reference sections

Use the **REST API** sidebar group for endpoint pages, request parameters, response schemas and interactive examples.
